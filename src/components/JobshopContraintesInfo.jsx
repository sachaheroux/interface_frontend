import React from 'react';

const JobshopContraintesInfo = () => {
    return (
        <div>
            <h2>Jobshop Contraintes Information</h2>
            <p>This algorithm uses constraint programming to solve the Jobshop scheduling problem. It optimizes the schedule to minimize the makespan, taking into account the due dates for each job.</p>
        </div>
    );
};

export default JobshopContraintesInfo; 